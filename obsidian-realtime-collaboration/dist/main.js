'use strict';

// Minimal plugin entry point (will be extended later)
class CollaborativePlugin {
    async onload() {
        // Will register editor extension and providers in later steps
    }
    onunload() {
        // Cleanup resources
    }
}

module.exports = CollaborativePlugin;
//# sourceMappingURL=main.js.map
